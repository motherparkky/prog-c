#ifndef ARRAY_ADVANCED_H
#define ARRAY_ADVANCED_H

#include <stddef.h>

int **one_dim_to_two_dim(int array[], size_t m, size_t n);
int *two_dim_to_one_dim(int *array[], size_t m, size_t n);

#endif /*  ! ARRAY_ADVANCED_H */
